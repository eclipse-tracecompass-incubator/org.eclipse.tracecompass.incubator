package org.eclipse.tracecompass.incubator.internal.shinro.tracetype.core.counters;

import org.eclipse.tracecompass.analysis.counters.core.CounterAnalysis;

public class ShinroCountersAnalysis extends CounterAnalysis {
    /* It appears that the base class, CounterAnalysis, might be sufficient for our case.
     * Just keeping this class defined in case we find out that it would be beneficial
     * to override some base class functionality
     */

}
